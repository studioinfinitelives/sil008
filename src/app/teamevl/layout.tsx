/** Scopes the Team EvL palette to this subtree, as `habisloth/layout.tsx` does. */
export default function TeamEvlLayout({ children }: LayoutProps<"/teamevl">) {
  return <div data-section="teamevl">{children}</div>;
}
